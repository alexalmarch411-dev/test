apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "web.fullname" . }}
  labels:
    {{- include "web.labels" . | nindent 4 }}
data:
  MONGO_TIMEOUT_MS: {{ .Values.env.mongoTimeoutMs | quote }}