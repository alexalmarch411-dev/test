apiVersion: v1
kind: Secret
metadata:
  name: {{ include "web.fullname" . }}
  labels:
    {{- include "web.labels" . | nindent 4 }}
type: Opaque
stringData:
  MONGO_URI: {{ printf "mongodb://%s:27017" (include "web.mongo.fullname" .) | quote }}